replicaCount: 2

image:
  repository: us-central1-docker.pkg.dev/applied-ridge-414907/my-docker-repo/hello-test-app
  tag: "v1.0.11"    # ← current latest, Image Updater will auto-update this going forward
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi